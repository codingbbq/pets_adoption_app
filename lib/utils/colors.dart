import 'package:flutter/material.dart';

const Color hdrBgColor = Colors.white; // Header background i,e appBar
const Color bgColor = Colors.white; 
const Color bodyBgColor = Color(0xFFF6F6F6); // Gray body background

const Color textColor = Color(0xFF616161); // Primary text color
const Color secondaryTextColor = Color(0xFF868686); // Secondary text color

const Color iconColor = Color(0xFFd2d2d2);

const Color themeColor = Color(0xFF306060);